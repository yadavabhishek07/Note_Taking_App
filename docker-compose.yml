version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: note_taking_postgres
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgrespassword
      POSTGRES_DB: note_taking_db
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d note_taking_db"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
